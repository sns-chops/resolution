// google ai generated
window.addEventListener('beforeunload', function (e) {
  // You can display a confirmation dialogue if needed (modern browsers have limitations on custom messages)
  // e.preventDefault(); 
  // e.returnValue = ''; 

  // Send a request to a server endpoint to register the event
  navigator.sendBeacon('/browser-closed', ''); 
});
